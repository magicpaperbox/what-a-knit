[project]
name = "what-a-knit"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "gunicorn>=23.0.0",
]

[dependency-groups]
dev = [
    "markdown>=3.10.2",
    "pdfkit>=1.0.0",
]

[tool.uv]
package = false
